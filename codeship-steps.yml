- type: serial
  dockercfg_service: dockercfg_generator
  steps:
    - service: workspace
      command: cp .env.test .env
    - service: workspace
      command: composer install
    - service: workspace
      command: ./vendor/bin/codecept run unit
    - service: workspace
      command: ./vendor/bin/codecept run api
    - name: deploy
      service: companies
#      tag: master|develop
      type: push
      image_name: registry.heroku.com/backend-company/web
      registry: registry.heroku.com
      dockercfg_service: dockercfg_generator